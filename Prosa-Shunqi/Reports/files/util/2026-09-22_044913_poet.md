# `util/poet.v`

**Current active status (2026-09-23): `ACCEPTED_V06_FILE_ROCQ90` — 1/1.**

Canonical report for authoritative Prosa v0.6 source file `util/poet.v`.
The leading timestamp records when the continuous translation-order run first
entered this file. Machine-readable acceptance truth is in
`Validation/planning/v06_pipeline/poet_module_{manifest,status}.json`.

## Source and scheduling

- Authoritative source: Prosa v0.6 commit
  `414e66760333eaa4ef78c685bcf53291c527a548`.
- Source SHA-256:
  `e14348ab5a0f3e262df7eb8702d34b4714e347f14c1f00bfbf24647bfc887c95`.
- Translation-order rank: 16; file-DAG layer 2.
- Direct internal dependency: `util/list.v`, already accepted.
- Public inventory: one lemma,
  `forall_exists_implied_by_forall_in_zip`.
- Migration classification: `NEW_IN_V06` / `NEW_TRANSLATION`; no historical
  Lean implementation was treated as a specification or reused wholesale.

## Translation and proof

The Lean statement retains the source's key representation choices:

- `eqType` becomes a carrier plus `DecidableEq`;
- `seq` remains ordered, multiplicity-preserving `List`;
- the Boolean predicate and `List.all` result remain `Bool`;
- the proposition-valued predicate remains `Prop`;
- witnesses come from a same-length zipped list, not a finite-set
  reformulation.

The proof chooses the source-style `idxOf` position of a member, obtains the
right-list element at that valid position, uses the already accepted `in_zip`
theorem to establish pair membership, and consumes `List.all = true`.
Production file: `Prosa/Util/Poet.lean`.

Pinned Lean compilation passed. `#print axioms` reports exactly the approved
standard foundations `propext` and `Quot.sound`; there is no `sorryAx`, custom
axiom, or unsafe proof escape.

## Semantic-validation history and findings

An actual-artifact type probe was performed before designing the bridge. It
confirmed that importing the compiled Lean statement produces the expected
SProp shell over imported `List`, `Prod`, `Bool`, `Exists`, `And`, membership,
length, zip, and all operations. This made it possible to validate the
theorem structurally rather than following its Lean proof dependencies.

The first computation-interface draft accidentally used monomorphic
`Type 0`, causing `lean4export` to materialize a second `List_inst1` carrier
that could not be identified with the target theorem's polymorphic `List`.
Changing the validation-only equations to explicit universes `u` and `v`
made the interfaces share the exact target representation. This is an
import/export representation lesson, not a production semantic change.

The reusable `PoetCorrespondence.v` layer now provides artifact-local
instances of already approved relations for:

- exhaustive Bool conversion, truth, Boolean equality, and conjunction;
- `eqType` equality evidence to imported `DecidableEq`;
- ordered `seq ↔ List`, membership, and length;
- source pairs ↔ imported `Prod`, pair lists, and zip;
- higher-order pair predicates and the actual imported `List.all` body;
- conjunction, implication, Iff, forall, and existential combinators.

The final theorem certificate composes these relations. It never invokes the
official theorem proof or the imported Lean theorem proof. The imported
theorem constant appears only in the exact target-type guard, specialized
through the approved representation maps.

### Source acquisition detail

Compiling the complete official `util/list.v` dependency under Rocq 9.3 hit a
stack overflow in the unrelated early proof `last0_cons`, even at the process
stack hard limit. The source theorem signature therefore uses the accepted
automatic extractor and the already recorded post-Section
`Check @declaration` evidence. The extractor omitted the irrelevant command
`Require Export prosa.util.list.` solely from the generated validation
signature; this transformation is explicit in metadata. The exact theorem
statement, binder structure, source block hash, source commit, and elaborated
type hash were not altered. The generated signature compiled independently.

| Declaration | Lean proof | Semantic status | Premises | Self-dependency | Acceptance |
|---|---|---|---|---|---|
| `forall_exists_implied_by_forall_in_zip` | PASS (`propext`, `Quot.sound`) | `CERTIFIED_WITH_PROP_SPROP_FOUNDATION` | none | source=false, target=false | `ACCEPTED_V06_TRANSLATION` |

Assumption audit:

- Prop/SProp boundary: `PropSPropFoundation.interpret_strict`;
- importer equality: `Lean.eq` plus audited definitional-UIP markers;
- validation-specific semantic premises: none;
- unexpected assumptions: none.

## Published evidence

- production source:
  `5ac29b131936a347c48314e533b53b2f9fd018908aa3db3c3bbcb8571469bb62`
- fresh `.olean`:
  `aed4c7e75af8b19fa1a5f5bd46cc2af5bf9d70d5cb494abfbab5cde6701e0e7a`
- export:
  `2859a11023659bac9912942c69519245448822d21658facabbcd68a6c563af86`
- imported Rocq `.vo`:
  `3ec9799f4e3e2988a33f728263ce165961a1827cec1e8b18abb5d4854a551771`
- certificate `.vo`:
  `2b920b180f4c6e2a1cae5069e98b055613178127e2523a742d7c805b5e4fd5b7`
- exact-type audit `.vo`:
  `77644975bc9e16b69bf2db72ad02d34dc447abbcf31f473ac7c852c6396aa6f7`
- source-acquisition metadata:
  `83c562a67d1d2d9c460d01ce351c7756cf3b962dc0721883c624a1c80c437843`
- fresh run directory:
  `Validation/.work/runs/translation_order_poet.sIjAZW`

## Final status — 2026-09-22 05:24:23

```text
util/poet.v = ACCEPTED_V06_FILE
declarations = 1 / 1 accepted
project = 16 / 357 files, 133 / 2439 declarations
translated-but-not-certified = 0
```

The central insight is that this higher-order theorem did not require proving
the Rocq theorem again or recreating the Lean proof. Once zip/all/membership
and predicate relations were isolated, its statement correspondence became a
composition over the already approved representation boundary.

## Rocq 9.0 migration / revalidation (2026-09-23)

The single declaration passed its fresh type guard, zip/all computation
boundary, certificate, assumption audit, and `rocqchk`. Source / Lean /
`.olean` / `.out` / imported `.vo` hashes are
`e14348ab5a0f3e262df7eb8702d34b4714e347f14c1f00bfbf24647bfc887c95`,
`5ac29b131936a347c48314e533b53b2f9fd018908aa3db3c3bbcb8571469bb62`,
`aed4c7e75af8b19fa1a5f5bd46cc2af5bf9d70d5cb494abfbab5cde6701e0e7a`,
`f0778c7668094265ab1841205cce9c5d0ccf4e39d9bcca28b564cbffa36f92e1`,
and `9d11e193ec8ea7822c7a09b94307b7d209800c44dfa4e67c5c4831bb0330aadd`.
The pair-predicate proof now uses explicit equality transport compatible with
Rocq 9.0 relevance; production Lean was unchanged. Trust fields are empty.

Final active status: `ACCEPTED_V06_FILE_ROCQ90` (1/1).
