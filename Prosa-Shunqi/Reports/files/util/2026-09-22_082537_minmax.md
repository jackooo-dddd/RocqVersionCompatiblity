# `util/minmax.v` canonical translation report

**Current active status (2026-09-23): `ACCEPTED_V06_FILE_ROCQ90` — 10/10.**

- Canonical report started: 2026-09-22 08:25:37 +08:00
- Authoritative source: Prosa v0.6 commit
  `414e66760333eaa4ef78c685bcf53291c527a548`
- Source file SHA-256:
  `a7ab36b3770500f8e136e51f8e324e54c6d8b741e368bc23ff8d87b240009830`
- Execution-order rank: 18
- File-DAG layer: 2
- Direct internal dependencies: `util/list.v`, `util/nat.v`,
  `util/notation.v`, `util/setoid.v` (all accepted before this work began)
- Public declaration inventory: 10 lemmas/corollaries

## 2026-09-22 08:25:37 — source contract and migration audit

The pinned file defines ten facts about conditional natural-number maxima:
four over an arbitrary ordered sequence, three over the canonical ordinal
interval, and three witness/subset facts.  The observable source computation
is MathComp's `maxn` big operator with identity zero.  Sequence order and
duplicates remain part of the input representation even though `maxn` makes
the final value insensitive to them.

Six declarations have high-confidence v0.4 Lean proof-strategy candidates,
but every one is classified `EVOLVED_SIGNATURE` / `ADAPT_OLD_LEAN`.  The old
helper used a propositional predicate and a `foldl`; v0.6 uses a Boolean
predicate and MathComp reflection.  Four witness/subset declarations are new
in v0.6.  Consequently the old file is not copied: the planned v0.6 helper is
a direct ordered-List `foldr Nat.max 0` with Boolean filtering semantics,
chosen to expose the same computation interface as the source big operator.

Planned reusable dependencies are the accepted eqType/DecidableEq,
ordered-seq/List, membership, filter/Bool truth, Nat order/equality, finite
range/ordinal, maximum-fold, existential/conjunction/implication, and
Prop/SProp correspondences.  The only anticipated new operation certificate
is the conditional maximum fold itself.  Current status is
`TRANSLATION_IN_PROGRESS`; no Minmax declaration is counted accepted.

## 2026-09-22 08:36:39 — whole-file Lean candidate compiles

All ten public v0.6 declarations now have Lean counterparts and the complete
`Prosa/Util/Minmax.lean` file compiles under Lean 4.33.1.  Six historical proof
ideas were adapted rather than copied, and the four v0.6-only witness/subset
facts were proved directly.  The production representation keeps predicates
as `Bool`, sequences as ordered `List`, and defines the source maximum
computation as `List.foldr` with `Nat.max` and identity zero.  A second marked
helper applies the same operation to `_root_.List.range n`, avoiding accidental
capture by Prosa's two-endpoint `List.range` declaration.

The main Lean proof insight is that one reusable witness lemma for a nonempty
Boolean-filtered maximum closes `bigmax_pred`, `bigmax_witness`, and the
strict-difference witness theorem.  This keeps the production proof graph
small while leaving semantic validation independent of those theorem proofs.
Current state: 10/10 translated and Lean-compiled; proof-axiom audit and all
actual-artifact semantic certificates remain pending, so accepted coverage is
unchanged at 146/2439.

## 2026-09-22 08:45:16 — validation-oriented computation interface compiled

The actual-artifact preflight showed that the ordinal-range maximum should not
inherit `_root_.List.range`'s implementation dependency graph merely for proof
convenience.  Before any Minmax result was accepted, the validation-only design
review therefore replaced the unaccepted `bigMaxNatRange` helper with a direct
primitive recursion on `Nat`.  Its zero and successor branches now expose the
same empty-range and append-the-new-ordinal computation used by MathComp's
`big_ord_recr`; the observable theorem statements and Boolean predicate
semantics are unchanged.

The revised whole production file compiles successfully under the pinned Lean
4.33.1 environment.  Production source SHA-256 is
`cb8a24168cad41f0e9f8be2b73c0ccafad5246be2f237da64a8a4cae90bafcb1`.
This is still a candidate milestone, not semantic acceptance: the fresh proof
audit, export/import, correspondence certificates, and fail-closed assumption
audit must be rerun against this new snapshot.  Published acceptance therefore
remains 146/2439.

## 2026-09-22 10:22:32 — all ten semantic certificates pass the pre-publication audit

The actual compiled Lean artifact has now been exported and imported into
Rocq, and a reusable `MinmaxCorrespondence.v` layer has been checked against
that imported representation. The layer relates MathComp `maxn`, the
conditional sequence big operator, Boolean `has`, canonical ordinal
enumeration, and Nat order/equality to the actual imported Lean `Nat.max`,
ordered-List fold, `List.any`, `Fin`, and direct-recursive `bigMaxNatRange`
computations. It does not invoke any of the ten source or target business
theorems.

All ten independent statement correspondence certificates and their separate
exact imported-type guards compile in Rocq 9.3. The informative source
`reflect` result of `bigmax_leq_seqP` was preserved as a Type-valued source
declaration and related structurally to the imported Lean proposition via the
new reusable `TypeSPropRel`; it was not silently weakened to an ordinary
source `Prop`.

The fail-closed `Print Assumptions` classifier reports, for every certificate:

- status `CERTIFIED_WITH_PROP_SPROP_FOUNDATION`;
- `semantic_premises = []`;
- source theorem dependency = `false`;
- target theorem dependency = `false`;
- unexpected assumptions = `[]`.

This is substantive kernel-checked pre-publication progress, but canonical
coverage remains 146/2439 until the same checks complete in a newly created
isolated work directory and the content-addressed manifest/status publication
gate succeeds.

## 2026-09-22 10:35:27 — `util/minmax.v` formally accepted

The publication gate reran the complete workflow from a newly created
isolated directory, `translation_order_minmax.1ZD0Tz`: fresh Lean dependency
and Minmax compilation, production proof audit, exact theorem-type export,
official source signature extraction, `rocq-lean-import`, all operation and
statement certificates, exact-type guards, marked `Print Assumptions`, frozen
baseline audit, and content-addressed publication.

All 10 declarations are `ACCEPTED_V06_TRANSLATION`; the file is
`ACCEPTED_V06_FILE`. Every semantic certificate is
`CERTIFIED_WITH_PROP_SPROP_FOUNDATION` with empty semantic premises, false
source and target theorem dependencies, and no unexpected assumptions. The
additional Type-to-SProp relation is kernel-proved and isolated in its own
module, so adding it did not alter the accepted shared `LogicalRelation.v`
hash used by Bigcat.

Key final hashes:

- production Lean source: `cb8a24168cad41f0e9f8be2b73c0ccafad5246be2f237da64a8a4cae90bafcb1`;
- fresh `.olean`: `52b1757ff3c027ebf442db6768fd7f1cba86e907991688aa4b648523a41cbb7b`;
- export: `edb01db632efbf330b26f4bf3ca1b916e42f21762a1c9e40a33431f955894859`;
- imported Rocq artifact: `e5771dcf0467cf03e6a948c4d491c890425ad0c9ca96ded7d6b02dfa15a62fb8`;
- semantic certificate `.vo`: `dfe435a169a89b0ef35ffed014a8d2c829e174bbb1586896450c9f24368326f3`;
- exact-type audit `.vo`: `e70cdfd0c3b8ec5a4e3eeada3626a78b95b6a0dff79c8bc54029992a2c92d3e1`;
- assumption-audit `.vo`: `555dc774ea5ce91e6c07ad270b8a7b8f11160d6160459c48acf12ec489ff548b`.

Cumulative machine-published coverage is now 18/357 files and 156/2439
declarations, with zero translated-but-not-certified debt.

## Rocq 9.0 migration / revalidation (2026-09-23)

After the current Rocq-9.0 Setoid acceptance gate, all **10/10** declarations
passed fresh guards, import, certificates, assumption audit, and `rocqchk`.
Source / Lean / `.olean` / `.out` / imported `.vo` hashes are
`a7ab36b3770500f8e136e51f8e324e54c6d8b741e368bc23ff8d87b240009830`,
`cb8a24168cad41f0e9f8be2b73c0ccafad5246be2f237da64a8a4cae90bafcb1`,
`52b1757ff3c027ebf442db6768fd7f1cba86e907991688aa4b648523a41cbb7b`,
`c4b69fda742a5acfbd67c76a4c2f8bbec9f0c8d0927bbe452c6ff0b9b62ba35e`,
and `900b967636d401b30868ae5ab476fd79b865b3fc4abae7fd259ddf041b1f421a`.
Rocq-9.0 repairs use explicit source equality transport and do not change
production Lean. Imported `propext` is classified as actual Lean foundation;
all unexpected assumption and kernel-trust fields are empty.

Final active status: `ACCEPTED_V06_FILE_ROCQ90`.
