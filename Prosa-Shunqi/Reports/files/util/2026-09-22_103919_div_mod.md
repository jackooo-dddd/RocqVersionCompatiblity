# `util/div_mod.v` canonical translation report

**Current active status (2026-09-23): `ACCEPTED_V06_FILE_ROCQ90` — 15/15.**

- Canonical report started: 2026-09-22 10:39:19 +08:00
- Authoritative source: Prosa v0.6 commit
  `414e66760333eaa4ef78c685bcf53291c527a548`
- Source file SHA-256:
  `7705c2f251bd96daab96e891b145ea8ca839b269c6f67f1bc954ba0219883b32`
- Execution-order rank: 19
- File-DAG layer: 2
- Direct internal dependencies: `util/nat.v`, `util/subadditivity.v`
  (both accepted before this work began)
- Public declaration inventory: 15 (13 lemmas, 2 definitions)

## 2026-09-22 10:39:19 — source contract and representation preflight

The authoritative file contains five new v0.6 division/modulo facts, the
computational `div_floor` and `div_ceil` definitions, and eight further facts
about ceiling division, subadditivity, multiples, floor bounds, and modular
subtraction. Only `div_floor`, `div_ceil`, and `mod_elim` have credible modern
historical Lean candidates; all are marked `ADAPT_OLD_LEAN` because their
v0.6 contracts changed. The other twelve declarations are new translations.

The central new semantic boundary is MathComp natural `divn`/`modn` and
Boolean divisibility versus Lean `Nat.div`/`Nat.mod` and decidable
divisibility. Validation will first bind the actual compiled equations and
prove reusable operation-level division, remainder, divisibility, and ceiling
correspondences. Natural subtraction remains truncated on both sides.

The initial design keeps `div_floor x y = x / y` and represents the source
Boolean divisibility branch of `div_ceil` by the corresponding decidable Lean
divisibility observation. This is a deliberate representation relation, not
an assumption that the two libraries are definitionally identical. Current
status is `TRANSLATION_IN_PROGRESS`; no DivMod declaration is counted accepted.

## 2026-09-22 10:56:07 — whole-file Lean candidate compiled

All 15 authoritative declarations now have production Lean candidates in
`Prosa/Util/Div_mod.lean`, in source order. The file compiles under the pinned
Lean 4.33.1 / Mathlib revision without `sorry`, `axiom`, or `unsafe`. The two
computational definitions deliberately preserve the source zero-divisor
behavior; in particular, `div_ceil` is not replaced wholesale by Mathlib's
`Nat.ceilDiv`, whose behavior at divisor zero is not the source contract.
Mathlib `ceilDiv` is used only inside proofs guarded by a positive divisor.

This milestone is translation/proof progress, not semantic acceptance. The
next validation stage must bind the actual compiled `Nat.div`, `Nat.mod`, and
divisibility interfaces, prove a reusable MathComp-to-Lean quotient/remainder
correspondence, and then compose the 15 declaration certificates. Until fresh
export/import and the fail-closed assumption audit pass, the accepted count
for `util/div_mod.v` remains 0/15 and project coverage remains 156/2439.

## 2026-09-22 11:27:51 — semantic work paused for workflow optimization

At the user's direction, no DivMod export/import or correspondence certificate
was attempted after the whole-file Lean compile milestone. The candidate is
preserved unchanged while the shared incremental validator is hardened. This
is neither a semantic failure nor acceptance: 15 declarations are translated
and Lean-compilable, 0 have current actual-artifact certificates, and none is
included in accepted coverage. Work resumes from the quotient/remainder
operation bridge after the optimization regression is complete.

## 2026-09-22 13:14:02 — all 15 correspondence proofs pass in a debug snapshot

The semantic proof phase is now complete at the kernel-checking level. A new
reusable `DivModCorrespondence.v` derives MathComp `divn`, `modn`, and Boolean
divisibility correspondence with the actual imported Lean `Nat.div`,
`Nat.mod`, and `Dvd` interface. The proof uses quotient/remainder equations,
remainder bounds, the zero-divisor cases, and truncated-natural subtraction;
it does not unfold the large imported `Nat.brecOn`/`Nat.below` implementation.

All 15 declaration certificates compile under the retired pre-9.0 validation baseline in the debug work
snapshot, including the two computational definitions and all 13 theorem
statements. Separate type guards show that the structural targets used by the
certificates are definitionally the exact imported theorem types. The
fail-closed assumption classifier reports, for every target:

- `semantic_premises = []`;
- `source_theorem_dependency = false`;
- `target_theorem_dependency = false`;
- `unexpected_assumptions = []`;
- status `CERTIFIED_WITH_PROP_SPROP_FOUNDATION` because the sole approved
  logical boundary is `PropSPropFoundation.interpret_strict` (in addition to
  recorded importer foundations).

This is substantive machine-checked progress, but it is not yet published
acceptance. An attempted resume of the interrupted exporter produced neither
an export artifact nor metadata, so that attempt is rejected rather than
silently reused. The final content-addressed prepare/finalize run must still
fresh-build, export, import, regress all 15 certificates, and publish the
manifest. Consequently the authoritative project count remains 156/2439 at
this milestone; 171/2439 will be recorded only if that final run succeeds.

## 2026-09-22 13:26:13 — `util/div_mod.v` formally accepted

The content-addressed validator completed its final prepare, check, and
publication gates for snapshot
`f525d2797714840b193125320d10766d3165655c9bfdf996e2bb42cd31593c8c`.
The prepare was genuinely fresh: Lean build 46.648 s, pinned-source
acquisition 3.701 s, actual-artifact export 9.991 s, and Rocq import 2.718 s.
The subsequent check and finalize runs verified the prepared hashes and
reported `VERIFIED_CACHE` for all four stages; neither recompiled Lean nor
re-exported/re-imported the artifact. Final certificate compilation took
5.864 s, assumption audit 0.125 s, and publication 0.372 s.

All 15 source declarations are now `ACCEPTED_V06_TRANSLATION`. Each semantic
certificate is `CERTIFIED_WITH_PROP_SPROP_FOUNDATION`, with empty semantic
premises, false source- and target-theorem dependencies, and no unexpected
assumption. The two computational definitions are included in the Lean axiom
audit: `div_floor` is axiom-free and `div_ceil` records the standard Lean
`propext` dependency induced by its decidable divisibility branch. No custom
axiom or proof escape was introduced.

Published content identities include production source
`c4deb5f9aba7cc58cd369d5f246aa15ec858fc0a06fa3adf3f8b3b683eafc37a`,
fresh `.olean`
`ef1d6b76b70933c51ab84a7d9ec7f275333e3daf61cefd9cbe1cc9fbf97f3634`,
Lean export
`78548787c1371ac5b34928c507a5e6582c5db3d88f2776aaddcf512e79d6b6ce`,
imported Rocq object
`0fb831b5253035a15d0beedb1c7df1babea8eaeb8987e8d784c5918d7f780a43`,
and target certificate object
`c816330f13db6b6c432bfb0b74599b51f27381d42413be8f99c950164aa2d525`.
Machine-readable authority is
`Validation/planning/v06_pipeline/div_mod_module_{manifest,status}.json`.

Final file status is `ACCEPTED_V06_FILE` (15/15). Cumulative coverage is now
19/357 files and 171/2439 declarations, with zero
translated-but-not-certified declarations.

## Rocq 9.0 migration / revalidation (2026-09-23)

All **15/15** declarations passed the formal Batch 3 pipeline. Official source
compiled directly on Rocq 9.0; actual `Nat.div`/`Nat.mod` computation remains
in the proof-complete boundary, with normal universe/elimination checking.
Source / Lean / `.olean` / `.out` / imported `.vo` hashes are
`7705c2f251bd96daab96e891b145ea8ca839b269c6f67f1bc954ba0219883b32`,
`c4deb5f9aba7cc58cd369d5f246aa15ec858fc0a06fa3adf3f8b3b683eafc37a`,
`68adb2f1956a67ca93bb24ae527ae29223ffddc8ba3e079c3b47a00cce5fb550`,
`845a33b3eb19732cba198b43100b26a24020d12ed203867e0d8f273d78b7c4f4`,
and `6d643a8d74d59e4dbacff783c955ab7f2fe6910aff95ad98d63c098d99a0a158`.
Only validation proof transport changed; production Lean was untouched.
`rocqchk` records imported `propext` as Lean foundation and reports no
type-in-type, unsafe fixpoint, assumed positivity, or custom Rocq axiom.
Assumption audit fields are all empty.

Final active status: `ACCEPTED_V06_FILE_ROCQ90`.
