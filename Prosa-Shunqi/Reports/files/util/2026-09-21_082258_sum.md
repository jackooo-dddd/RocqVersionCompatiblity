# `util/sum.v`

Canonical report timestamp inherited from first historical report:
**2026-09-21 08:22:58**.

This file is the next unfinished file in the approved execution order. It is
dependency-ready because its direct internal dependencies `util/nat.v`,
`util/notation.v`, and `util/rel.v` are all `ACCEPTED_V06_FILE`.

## Current result

- Authoritative source: Prosa v0.6 commit
  `414e66760333eaa4ef78c685bcf53291c527a548`.
- Source SHA-256:
  `3415b2c1d3d7ed892a168d114f66007913a014a1ec3ede2ba8fef84217bb14d5`.
- Production candidate: `Prosa/Util/Sum.lean`.
- Current status: **`PARTIAL_V06_FILE`**, 3/25 accepted.
- The current 22 unfinished declarations are not translated candidates, so
  current translated-but-not-certified debt remains zero.

| Accepted declaration | Certificate | Semantic status |
|---|---|---|
| `sum_of_ones` | `sum_of_ones_statement_certificate` | `CERTIFIED_WITH_PROP_SPROP_FOUNDATION` |
| `big_nat_eq0` | `big_nat_eq0_statement_certificate` | `CERTIFIED_WITH_PROP_SPROP_FOUNDATION` |
| `sum_le_summation_range` | `sum_le_summation_range_statement_certificate` | `CERTIFIED_WITH_PROP_SPROP_FOUNDATION` |

For all three, `semantic_premises=[]`, source and target theorem dependencies
are false, and no unexpected assumption is present. They reuse a certified
finite Nat interval-sum value bridge that preserves the half-open `[m,n)`
semantics. The theorem shell exposes the approved `interpret_strict`
foundation.

Accepted-cluster hashes: Lean source
`36a5281c82a0ed9d6b6d087a20b81c85fba49ce31bd1c654cf1bc6cffacef790`,
fresh `.olean`
`a3709f824b76b6e7fc2b7cd99761ba298a7015757d331b195c40d9b780bb63f2`,
export `1d05393f0c197ac66252307b0624a39def045df5eff9facd61c9d10eb3991bd6`,
imported `.vo`
`8809b61c99f9b853b2d112f5f595a0012e99ddf627b61c4c9dd1cb21dd9c4a36`,
and source-validation `.vo`
`1f861ee3bab48bcee215ea27a3891bfe05cc8957689add2da9b0a50e09c63e59`.

## Unfinished authoritative inventory

The 22 declarations below remain `NOT_YET_VALIDATED` / `NOT_ACCEPTED`; this is
not a semantic-failure classification:

```text
sum_nat_eq0_nat
sum_nat_gt0
sum_majorant_constant
sum_split_exhaustive_mutually_exclusive_preds
bigmax_leq_sum
sum_le_subseq
leq_sum_seq
eq_sum_seq
leq_sum_seq_pred
leq_sum_subseq
leq_sum_sub_uniq
ltn_sum_leq_seq
eq_sum_leq_seq
big_sum_eq_in_eq_sized_intervals
sum_over_partitions_le
reorder_summation
sum_over_partitions_eq
sum_leq_mono
sum_unit1
pigeonhole_on_interval
sum_ge_2_seq
sum_ge_2_nat
```

The migration audit marks `leq_sum_sub_uniq` and `reorder_summation` as
`REVIEW_REQUIRED`; this is a representation/reuse review gate, not proof of a
translation mismatch.

## History

- **2026-09-21 08:22:58:** Sum was initially deferred until `util/nat.v`
  became an accepted file. This enforced the authoritative file DAG instead
  of treating a missing declaration-level edge as permission to start.
- **09:42:13:** that file-DAG dependency gate was satisfied after Nat's
  subtraction correspondence and whole-file publication passed.
- **12:00:20:** the first interval-sum candidates compiled. The candidate
  representation kept interval sums separate from sequence sums and filtered
  sums; no blanket “all sums are Finset sums” rule was introduced.
- **12:39:32:** fresh actual-artifact validation and publication accepted
  the three declarations above. The source file correctly remained partial;
  the other 22 were not silently dropped. The reusable bridge relates
  MathComp's half-open bigop interval to the actual imported
  `List.range'`/`map`/`foldr` computation underlying `Finset.Ico.sum`, preserving
  endpoints, order, multiplicity, and the empty interval.
- The normalized target types were accepted only after both `Meta.isDefEq` and
  a Lean-kernel equality guard tied them to the current compiled theorem
  types. A validation-only mutation changing the fold identity from zero to
  one was rejected, providing a concrete check against false-positive
  normalization.
- Subsequent List-only work did not change Sum. `leq_sum_sub_uniq` and
  `reorder_summation` remain explicit representation/reuse review gates; the
  file's 3/25 status is therefore a deliberate partial result, not a stalled
  theorem mislabeled as failure.

### 2026-09-22 00:09:12 +08:00 — Sequence/filter candidate cluster

The first thirteen previously unfinished source declarations, from
`sum_nat_eq0_nat` through `eq_sum_leq_seq`, now have proof-clean Lean
candidates. The implementation deliberately separates ordered list sums,
filtered list sums, filtered maxima, and Boolean subsequence recognition via
the `LEAN_HELPER` interfaces `sumSeq`, `sumFiltered`, `maxFiltered`, and
`subseqb`. This retains sequence order and multiplicity instead of coercing
these declarations to finite-set sums.

The pinned Lean command `lake env lean Prosa/Util/Sum.lean` passed for this
intermediate snapshot. No semantic acceptance is claimed yet: the file is
still changing, and fresh artifact export/import, source-to-target
correspondence certificates, assumption audit, and publication remain
mandatory. The canonical machine status therefore remains 3/25 accepted.

### 2026-09-22 00:21:13 +08:00 — Whole-file Lean snapshot frozen

The remaining nine declarations were translated after the sequence/filter
cluster. They preserve three distinct aggregation domains: ordered List sums
with multiplicity, filtered partition Lists, and half-open `Finset.Ico`
intervals. In particular, partition names are still an ordered sequence (and
may repeat unless the source supplies `uniq`); they were not silently changed
to a finite set.

All 25 source counterparts now compile together. `lake build
Prosa.Util.Sum` passed, and a fail-closed transitive `#print axioms` audit
passed for every theorem with only the project-approved Lean foundations.
The frozen candidate hashes are Lean source
`57497c4f2411a16582a95cd8992456178d6791e442f70f4365c2c1cd93f53361`
and project `.olean`
`93694c6e11cfc9c3f616473425022f334a526eb96f365ee14dca613ec929797c`.

This milestone is deliberately recorded as **translated + proof-clean**, not
accepted. The 22 new declarations still require fresh isolated compilation,
actual-artifact export/import, semantic correspondence certificates, Rocq
assumption audit, and publication. Machine acceptance remains 3/25.

### 2026-09-22 01:17:54 +08:00 — Sequence computation interface imported

The frozen production snapshot was exported through a validation-only
interface whose equations refer directly to the actual compiled
`Prosa.Util.Sum` definitions. Rocq 9.3 imported the resulting artifact
successfully. The exported artifact hash is
`8e0c9a484ef63a54cc8b42a83263a7f1223ae166f9f8b7067cb30b72ec717fab`,
and the imported `.vo` hash is
`b39d296602389b295890c723689ac2b704db6440b8b3506b92bf6e6c7aa0b4a8`.

`SumSequenceCorrespondence.v` establishes reusable correspondence for the
artifact-local List, Bool, Nat, equality, membership, filtering, length,
Boolean folds, ordered sequence sum, filtered sum/max, and `subseqb`
interfaces. This local instantiation is necessary because separate
`lean4export` artifacts generate distinct imported carrier namespaces; it
reuses the approved relations rather than inventing a new representation.

Independent semantic certificates and separate exact-target-type guards now
compile for `sum_nat_eq0_nat` and `sum_nat_gt0`. The semantic proofs are
structural compositions of lower-level correspondences and do not invoke the
official source theorem proof or imported Lean target theorem. They are not
yet counted as accepted: fail-closed assumption classification and formal
cluster publication are still outstanding, so machine acceptance remains
3/25.

### 2026-09-22 01:38:41 +08:00 — Sequence certificate composition scales to nine targets

The first two certificates passed the automatic fail-closed assumption
classifier with empty semantic premises, false source/target theorem
self-dependency, and no unexpected assumptions. Their only non-kernel logical
boundary is the already approved `PropSPropFoundation.interpret_strict`.

The reusable correspondence layer was then extended with implication,
pointwise membership/predicate/order, Boolean Nat equality, subsequence truth,
and filtered-sum order/equality combinators. Using those components, Rocq now
accepts semantic certificates and separate exact imported-type guards for
nine sequence/filter declarations: `sum_nat_eq0_nat`, `sum_nat_gt0`,
`sum_majorant_constant`, `bigmax_leq_sum`, `sum_le_subseq`,
`leq_sum_subseq`, `leq_sum_seq`, `eq_sum_seq`, and `leq_sum_seq_pred`.

This is evidence of correspondence-DAG reuse rather than nine isolated proof
replays: the later theorem certificates mostly instantiate and compose the
same operation relations. Formal publication waits for the remaining four
members and a unified assumption audit, so the canonical accepted count has
not yet moved from 3/25.

Legacy source report:
`Reports/legacy/2026-09-21_082258_HKT_utility_foundation_expansion_report.md`,
SHA-256 `814f697e7dbc8757729d2c2c0905ded96976407db8ab97e67abdd53b330686bb`.
## 2026-09-22 02:29:32 +08:00 — Partition-sum bridge milestone

- The actual compiled Sum artifact was expanded to cover the remaining
  sequence-side theorem types and the production partition helper equations;
  no production definition or theorem statement was changed.
- `sum_over_partitions_le`, `reorder_summation`, and
  `sum_over_partitions_eq` now have independent structural correspondence
  certificates using the shared List/Bool/Nat, filtered sum, nested partition
  sum, coverage, and `uniq`/`Nodup` relations.
- Separate exact-type guards reference the actual imported Lean theorem
  constants; the semantic proofs reference neither the source theorem proofs
  nor the target theorem proofs.
- Fail-closed assumption classification passed for all three with
  `semantic_premises=[]`, both self-dependency flags `false`, and
  `unexpected=[]`. Their classification is
  `CERTIFIED_WITH_PROP_SPROP_FOUNDATION`.
- Current work-path semantic progress is 19/25, but the canonical published
  count remains 16/25 until all sequence certificates are republished against
  the expanded export/import hashes and the cluster gate completes.

## 2026-09-22 02:48:45 +08:00 — Sequence-side semantic closure

- Closed `sum_leq_mono` with reusable higher-order Nat-function/family and
  monotonicity correspondences.
- Closed `sum_unit1` against the exact imported `Finset.univ` expression using
  an explicit Rocq-unit/imported-Lean-Unit relation and the two actual finite
  enumeration computations.
- Closed `sum_ge_2_seq` compositionally through existing list, membership,
  `uniq`/`Nodup`, Nat order/sum, Boolean equality, existential, and conjunction
  relations. No source or target business theorem proves its own certificate.
- All three have separate actual imported-type guards and clean fail-closed
  assumption audits. The only logical trust label remains the approved
  Prop/SProp foundation; the Unit/Finset target additionally exposes standard
  importer foundations (`Classical_choice`, `Quot_sound`, `propext`).
- Work-path semantic progress is 22/25; the final three interval declarations
  remain. No new production translation or proof debt was introduced.

## 2026-09-22 03:24:30 +08:00 — Final interval certificates compiled

- `big_sum_eq_in_eq_sized_intervals`, `pigeonhole_on_interval`, and
  `sum_ge_2_nat` now each have independent Rocq correspondence certificates
  over the actual imported Lean artifact and the automatically extracted
  official v0.6 statement.
- The new shared operations cover arbitrary related interval endpoints and
  Nat functions, Bool-predicate conversion and `Bool.toNat`, Boolean
  conjunction, Nat addition/truncated subtraction/order, reflected Nat
  equality, and nested existential witnesses. The theorem certificates reuse
  these operations rather than invoking either theorem proof.
- All exact imported-type guards compile. The source-side
  `pigeonhole_on_interval` guard reduces the MathComp Bool coercion to the
  explicit 0/1 representation, confirming that no hand-written approximation
  replaced the official statement.
- Automatic assumption audits for the three targets pass fail-closed with no
  semantic premise, no source/target theorem self-dependency, and no
  unexpected assumption. Their current classification is
  `CERTIFIED_WITH_PROP_SPROP_FOUNDATION`.
- All 25 declaration correspondences now kernel-check on the work path. The
  file remains formally `PARTIAL_V06_FILE` only until current-artifact full
  regression and publication complete; historical 16/25 publication evidence
  is not being treated as evidence for the expanded snapshot.

## 2026-09-22 03:37:19 +08:00 — Whole-file acceptance

`util/sum.v` is now `ACCEPTED_V06_FILE` with all 25 public declarations
accepted. The final gate used two complementary current-snapshot artifacts:

- Interval artifact: six exact target statements, each with a Lean-kernel
  definitional-equality guard around the finite-sum projection. Export hash
  `b5c74781c14175c6df1649380e8e86912757a975e07b0ce06121df3fbf870483`;
  imported object hash
  `896098c0eecff17d2db632ec232d56003458a5fb66404aff26ccc8e3072cb320`;
  certificate object hash
  `0a60cc2b03a4e4812dff7c0d0cd8c0d382e82c147771fcc7a0660fdc512b94ec`.
- Sequence artifact: nineteen exact target statements plus the actual compiled
  operation/equation interface for ordered Lists, filters, sums, maxima,
  subsequences, and partitions. Export hash
  `8ab4c41eed5588a3992de8e8376a06fa0bbcfa7ee36414259580d871320ec9be`;
  imported object hash
  `3050dc00d1f7dc06cf95c7b60db8b44ec7297f99a39cd8b5a7a9c3701ad01e45`;
  certificate object hash
  `b2cb42779106d4699a694f6635745cf3359360b346e5e0c59d7678dcc70450aa`.

The clean sequence run recompiled the pinned production dependency path,
source extraction, export/import, all common correspondences, all certificate
modules, exact-type guards, and marked assumption audit. The interval run also
reran the incorrect-fold-identity negative control. Across all 25 theorem
certificates: `semantic_premises=[]`, source theorem dependency `false`, target
theorem dependency `false`, and `unexpected=[]`. The approved
`PropSPropFoundation.interpret_strict` boundary remains visible where required;
the finite interval value bridge itself is kernel-certified without it.

The reusable correspondence DAG now covers ordered and filtered sequence
sums, finite half-open interval sums, Bool-to-Nat counts, max, membership,
subsequence recognition, partitioned nested sums, `uniq`/`Nodup`, higher-order
Nat function families and monotonicity, Unit enumeration, Nat arithmetic and
order, and existential/conjunctive witness shells. This is the final
acceptance result, not an intermediate batch PASS.
