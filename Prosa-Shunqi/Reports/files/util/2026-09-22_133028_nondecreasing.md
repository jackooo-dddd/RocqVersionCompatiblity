# `util/nondecreasing.v` canonical translation report

- Canonical report started: 2026-09-22 13:30:28 +08:00
- Authoritative source: Prosa v0.6 commit
  `414e66760333eaa4ef78c685bcf53291c527a548`
- Source file SHA-256:
  `34c20d63e8e9bf22691466a247704518093ea66e8e89ed3c913891ba425b93a3`
- Execution-order rank: 20
- File-DAG layer: 2
- Direct internal dependencies: `util/epsilon.v`, `util/list.v`, `util/nat.v`
  (all accepted before this work began)
- Public declaration inventory: 33 (3 definitions and 30 propositions)

## 2026-09-22 13:30:28 — selection and source-contract preflight

The file is the first READY unfinished entry after accepted rank 19
`util/div_mod.v`. Its three computational definitions describe nondecreasing
and strictly increasing default-indexed Nat lists and the adjacent truncated
differences of a list. Downstream statements cover filtered half-open ranges,
deduplication of sorted sequences, reconstruction from distances, maximum
distance bounds, and filtered-iota/dedup behavior.

The migration table gives three `REUSE_AFTER_REVALIDATION`, twenty-nine
`ADAPT_OLD_LEAN`, and one `NEW_TRANSLATION` row. A direct v0.4→v0.6 source diff
shows that the old file is useful proof memory but not specification. A
read-only compile probe of the historical Lean candidate against the accepted
new utility modules fails immediately because the new `util/list.v` correctly
does not expose the historical Lean-only `nthD` helper; later failures also
show changed dedup simplification behavior and the v0.6-only
`distances_of_iota_ε` boundary. No historical file was copied or modified.

The production translation will therefore use the approved `List` carrier
and actual `List.getD n 0` computation directly. Work is partitioned into four
source-order semantic clusters: definitions/order, dedup, distance
computation, and higher distance properties. The validation plan reuses the
already certified Nat/order/subtraction, ordered List, membership, filter,
length, append, range/iota, getD, dedup, max0/last0, and logical bridges; only
the adjacent-distance operation correspondence should need a new reusable
layer. No declaration is accepted at this milestone. Project coverage remains
19/357 files and 171/2439 declarations.

## 2026-09-22 13:52:03 — whole-file Lean translation and proof-clean audit

All 33 public v0.6 declarations now have production candidates in
`Prosa/Util/Nondecreasing.lean`. The file compiles as a whole under the pinned
Lean 4.33.1 / Mathlib environment, and `lake build
Prosa.Util.Nondecreasing` completed successfully. The implementation preserves
the source's ordered `seq nat` representation as `List Nat`, zero-defaulted
indexing as `List.getD`, truncated Nat subtraction, Bool-backed filtering, and
deduplication. A private reducible `nthD` spelling is recorded as a
`LEAN_HELPER`; it introduces no new semantics and remains subject to the
actual-artifact export preflight.

The first fail-closed axiom pass exposed an infrastructure issue rather than a
Lean proof failure: Lean wrapped five long `#print axioms` results across
multiple lines, while the shared parser accepted only one-line results. The
parser was extended to collect a result through its closing bracket, reject
malformed trailing text, and explicitly reject truncated records. Replaying
the exact production log now gives 33/33 `PASS`, with only the configured
standard Lean foundations (`propext`, `Classical.choice`, and `Quot.sound`)
where used. Regression against the current accepted `util/div_mod.v` log gives
15/15 `PASS`; a deliberately truncated Nondecreasing log is rejected with
`AUDIT_TRUNCATED`.

This milestone establishes `TRANSLATED` and `PROOF_CLEAN`, not semantic
acceptance. No declaration count is added to coverage yet. The next gate is a
fresh compiled-artifact export/import preflight followed by the four semantic
certificate batches recorded in the selection manifest.

## 2026-09-22 14:11:03 — first actual-artifact import preflight

The first exact export covered all 33 production declarations plus one
comprehensive validation-only computation interface. Lean compilation and
`lean4export` succeeded; the resulting `Nondecreasing.out` was 1,189,598 bytes
with 65 explicit export roots (30 statement-only target theorems, four
definition bodies, and 31 interface proofs). Rocq import did not complete:
`rocq-lean-import` exited with code 139 after reaching declaration 17,298 in
an unrelated `UInt32`/string representation dependency chain.

This is classified as an export-interface dependency explosion, not a
semantic mismatch and not a source-translation proof failure. Inspection
showed that several convenience equations had tactic-generated (`simp` or
library theorem) proof bodies; exporting those bodies recursively retained a
large Mathlib proof graph. The next attempt will keep the same compiled
production target types and definitions but shrink the validation interface
to definitional/constructor equations with minimal proof terms. Any remaining
non-definitional equation will be statement-only only when backed by an
already accepted lower-level correspondence and will remain visible in the
dependency audit. Acceptance coverage remains unchanged at 19/357 files and
171/2439 declarations.

## 2026-09-22 15:09:20 — actual-artifact import blocker closed

Binary-searching the reduced export roots separated two independent causes.
`distances` imported by itself, while every definition containing the source's
zero-defaulted lookup retained `List.getD` and its implementation closure. A
guarded `Meta.reduceAll` projection exposes the exact compiled computation as
`Option.rec ... (List.get?Internal xs n)`. This removed the dependency
explosion, but also exposed a real importer limitation: `rocq-lean-import`
classified `Option` globally as potentially SProp-squashed and therefore did
not generate the Type-valued `Option.rec` needed by the concrete `Option Nat`
universe instance.

The workspace-local importer now decides recursor availability from the
current instantiated result sort. With explicit upfront universe
instantiation in this import fixture, the isolated `nthD` artifact imports
successfully. The complete 33-target normalized export initially still exited
139 near `Char.ofNat`; rerunning with the repository's established Rocq stack
limit (`ulimit -s 65520`) imports the complete artifact successfully: 369
entries, 776 possible instances, 31 universe expressions, and 8,026 expression
nodes. Thus the failure was importer/host-stack behavior, not a semantic
mismatch in the translated declarations.

Four definition-body normalizations (`nondecreasing_sequence`,
`increasing_sequence`, `distances`, and the public validation spelling of
`nthD`) now have validation-only Lean kernel guards. The guards connect the
actual compiled bodies to the exact exported normal forms; their axiom audit
contains only the already allowed Lean foundations (`propext`,
`Classical.choice`, and `Quot.sound`) where applicable, and no `sorryAx`.
The importer fix has been folded into the content-addressed tooling patch and
will be regression-tested before publication. No semantic certificate has yet
been counted accepted, so formal coverage remains 19/357 files and 171/2439
declarations.

## 2026-09-22 17:14:18 — source fidelity and computation bridge milestone

The source extractor now handles Unicode Rocq identifiers and active Section
notations. All 33 declarations were acquired from the pinned v0.6 file and the
generated source module compiles in the retired pre-9.0 validation baseline: the three computational
definitions are `BODY_EXACT`, and the 30 theorem declarations are
`STATEMENT_EXACT_PROOF_OMITTED` with elaborated-type evidence. The source
module hash is
`34c20d63e8e9bf22691466a247704518093ea66e8e89ed3c913891ba425b93a3`.
The official legacy `util/list.v` proof of `last0_cons` overflows the retired pre-9.0 validation baseline's
stack, so source acquisition drops that import and reconstructs only the exact
source-local transparent bindings `max0`, `first0`, and `last0`; it does not
alter any Nondecreasing declaration statement or body.

A stale-artifact check found that invoking `lean` without `-o` had typechecked
the validation interface without replacing its Lake `.olean`. The interface
was rebuilt explicitly; its current hash is
`ef82d73ac17e9163c882e0489567db81cf19f0e60377c214f1877be1b2d4e5f8`.
The three `nthD` equations are now kernel-`rfl` proofs. The resulting export
keeps only the 30 business theorem roots statement-only and exports five
computation equations with their real proof bodies. It is 499,500 bytes with
SHA-256
`81516f34dda44dbbe6cbcb23a5d1b12a36135884118aeb9e9f83a63a385ad2f6`.
The correctly namespaced imported `.vo` has SHA-256
`5a7e8b3a28e8a114738a4a2e27d20c56cb2906121e0325c2d85c4911c74b6992`.

Most importantly, the actual imported `nthD` computation equations now depend
only on the visible importer equality/UIP and `propext` foundations; the prior
transitive statement-only Lean theorem assumptions are gone. A generated
artifact-local Bool/List adapter and a new Nat-List operation layer compile in
Rocq. The latter kernel-checks list roundtrips, Nat `<=`/`<`, length, and
zero-defaulted `nthD` correspondence against this exact imported artifact.
There are no validation-specific semantic premises. This is reusable
operation-level progress, not declaration acceptance: all 33 production
candidates remain unpublished until the predicate/`distances` correspondences,
30 independent theorem-statement certificates, and whole-file audit close.
